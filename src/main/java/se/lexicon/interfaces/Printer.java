package se.lexicon.interfaces;

@FunctionalInterface
public interface Printer { //interface with ONE param

    // Printer & Conditional
    void print(String value); //takes param and then print it -> void: no return type
}
